{
  "name": "imade",
  "version": "2.8.1",
  "description": "Personal song library with comparison ranking",
  "main": "electron.js",
  "scripts": {
    "dev": "vite",
    "dev:server": "node server.js",
    "build:client": "vite build",
    "start": "node server.js",
    "electron": "electron .",
    "build": "vite build && electron-builder --win",
    "build:mac": "vite build && electron-builder --mac",
    "build:linux": "vite build && electron-builder --linux",
    "migrate": "node src/migrate.js",
    "build:all": "vite build && electron-builder -mwl"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^12.6.2",
    "express": "^4.18.2",
    "express-session": "^1.19.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.3",
    "electron": "^33.0.0",
    "electron-builder": "^25.1.8",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "vite": "^7.3.1"
  },
  "build": {
    "appId": "com.berrynotes.imade",
    "productName": "iMade",
    "asar": true,
    "asarUnpack": [
      "node_modules/better-sqlite3/**"
    ],
    "win": {
      "target": "zip",
      "artifactName": "iMade-win.${ext}"
    },
    "mac": {
      "target": "zip",
      "category": "public.app-category.music",
      "artifactName": "iMade-mac.${ext}"
    },
    "linux": {
      "target": [
        "AppImage",
        "zip"
      ],
      "category": "Audio",
      "artifactName": "iMade-linux.${ext}"
    },
    "files": [
      "electron.js",
      "preload.js",
      "server.js",
      "src/db.js",
      "src/middleware/**/*",
      "dist-client/**/*",
      "build/icon.png",
      "package.json",
      "node_modules/**/*"
    ]
  }
}
